// Code generated by the Encore v1.58.0 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-shipment-tracker-ysci.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the shipment-tracker-ysci Encore application.
 */
export default class Client {
    public readonly auth: auth.ServiceClient
    public readonly automation: automation.ServiceClient
    public readonly customers: customers.ServiceClient
    public readonly extraction: extraction.ServiceClient
    public readonly invoicing: invoicing.ServiceClient
    public readonly quotes: quotes.ServiceClient
    public readonly sales: sales.ServiceClient
    public readonly shipments: shipments.ServiceClient
    public readonly warehouse: warehouse.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.auth = new auth.ServiceClient(base)
        this.automation = new automation.ServiceClient(base)
        this.customers = new customers.ServiceClient(base)
        this.extraction = new extraction.ServiceClient(base)
        this.invoicing = new invoicing.ServiceClient(base)
        this.quotes = new quotes.ServiceClient(base)
        this.sales = new sales.ServiceClient(base)
        this.shipments = new shipments.ServiceClient(base)
        this.warehouse = new warehouse.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: lib.AuthParams | AuthDataGenerator
}

export namespace lib {
    export interface AuthParams {
        authorization: string
    }
}

export namespace auth {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.authLogin = this.authLogin.bind(this)
            this.authMe = this.authMe.bind(this)
            this.columnTemplatesDelete = this.columnTemplatesDelete.bind(this)
            this.columnTemplatesList = this.columnTemplatesList.bind(this)
            this.columnTemplatesUpsert = this.columnTemplatesUpsert.bind(this)
            this.companyList = this.companyList.bind(this)
            this.companyProvision = this.companyProvision.bind(this)
            this.companyUserCreate = this.companyUserCreate.bind(this)
            this.companyUserDelete = this.companyUserDelete.bind(this)
            this.companyUserUpdate = this.companyUserUpdate.bind(this)
            this.companyUsersList = this.companyUsersList.bind(this)
            this.userCreate = this.userCreate.bind(this)
            this.userDelete = this.userDelete.bind(this)
            this.userUpdate = this.userUpdate.bind(this)
            this.usersList = this.usersList.bind(this)
        }

        public async authLogin(params: controllers.AuthLoginRequest): Promise<controllers.AuthLoginResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/login`, JSON.stringify(params))
            return await resp.json() as controllers.AuthLoginResponse
        }

        public async authMe(params: controllers.AuthMeRequest): Promise<controllers.AuthMeResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.authorization,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/auth/me`, undefined, {headers})
            return await resp.json() as controllers.AuthMeResponse
        }

        public async columnTemplatesDelete(id: string, params: controllers.DeleteColumnTemplateRequest): Promise<controllers.DeleteColumnTemplateResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.authorization,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/column-templates/${encodeURIComponent(id)}`, undefined, {headers})
            return await resp.json() as controllers.DeleteColumnTemplateResponse
        }

        public async columnTemplatesList(params: controllers.ListColumnTemplatesRequest): Promise<controllers.ListColumnTemplatesResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.authorization,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/column-templates`, undefined, {headers})
            return await resp.json() as controllers.ListColumnTemplatesResponse
        }

        public async columnTemplatesUpsert(params: controllers.UpsertColumnTemplateRequest): Promise<controllers.UpsertColumnTemplateResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                columns: params.columns,
                name:    params.name,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/column-templates`, JSON.stringify(body), {headers})
            return await resp.json() as controllers.UpsertColumnTemplateResponse
        }

        /**
         * Platform-only: lists every company. Company admins never see other companies.
         */
        public async companyList(): Promise<controllers.CompanyListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/companies`)
            return await resp.json() as controllers.CompanyListResponse
        }

        /**
         * Platform-only: create a new company and seat its first admin in one step.
         */
        public async companyProvision(params: controllers.CompanyProvisionRequest): Promise<controllers.CompanyProvisionResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/companies`, JSON.stringify(params))
            return await resp.json() as controllers.CompanyProvisionResponse
        }

        public async companyUserCreate(companyId: string, params: controllers.CompanyUserCreateRequest): Promise<controllers.CompanyUserCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/companies/${encodeURIComponent(companyId)}/users`, JSON.stringify(params))
            return await resp.json() as controllers.CompanyUserCreateResponse
        }

        public async companyUserDelete(companyId: string, id: string): Promise<controllers.CompanyUserDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/companies/${encodeURIComponent(companyId)}/users/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.CompanyUserDeleteResponse
        }

        public async companyUserUpdate(companyId: string, id: string, params: controllers.CompanyUserUpdateRequest): Promise<controllers.CompanyUserUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/companies/${encodeURIComponent(companyId)}/users/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as controllers.CompanyUserUpdateResponse
        }

        public async companyUsersList(companyId: string): Promise<controllers.CompanyUsersListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/companies/${encodeURIComponent(companyId)}/users`)
            return await resp.json() as controllers.CompanyUsersListResponse
        }

        /**
         * Admins and managers add users to their OWN company; companyId comes from the token.
         */
        public async userCreate(params: controllers.UserCreateRequest): Promise<controllers.UserCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/users`, JSON.stringify(params))
            return await resp.json() as controllers.UserCreateResponse
        }

        /**
         * Deactivates (soft-deletes) a user within the caller's company. You can't deactivate
         * yourself, so a company can't accidentally lock out its last admin from this call.
         */
        public async userDelete(id: string): Promise<controllers.UserDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/auth/users/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.UserDeleteResponse
        }

        public async userUpdate(id: string, params: controllers.UserUpdateRequest): Promise<controllers.UserUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/auth/users/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as controllers.UserUpdateResponse
        }

        public async usersList(): Promise<controllers.UsersListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/auth/users`)
            return await resp.json() as controllers.UsersListResponse
        }
    }
}

export namespace automation {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.automationLogs = this.automationLogs.bind(this)
            this.automationTrigger = this.automationTrigger.bind(this)
        }

        public async automationLogs(shipmentId: string): Promise<controllers.AutomationLogsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/automation/${encodeURIComponent(shipmentId)}/logs`)
            return await resp.json() as controllers.AutomationLogsResponse
        }

        public async automationTrigger(params: controllers.AutomationTriggerRequest): Promise<controllers.AutomationTriggerResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/automation/trigger`, JSON.stringify(params))
            return await resp.json() as controllers.AutomationTriggerResponse
        }
    }
}

export namespace customers {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.aresLookup = this.aresLookup.bind(this)
            this.contactCreate = this.contactCreate.bind(this)
            this.contactDelete = this.contactDelete.bind(this)
            this.contactList = this.contactList.bind(this)
            this.contactUpdate = this.contactUpdate.bind(this)
            this.customerCreate = this.customerCreate.bind(this)
            this.customerDelete = this.customerDelete.bind(this)
            this.customerGet = this.customerGet.bind(this)
            this.customerList = this.customerList.bind(this)
            this.customerUpdate = this.customerUpdate.bind(this)
            this.documentContent = this.documentContent.bind(this)
            this.documentCreate = this.documentCreate.bind(this)
            this.documentDelete = this.documentDelete.bind(this)
            this.documentList = this.documentList.bind(this)
            this.invoiceCreate = this.invoiceCreate.bind(this)
            this.invoiceDelete = this.invoiceDelete.bind(this)
            this.invoiceList = this.invoiceList.bind(this)
            this.invoiceUpdate = this.invoiceUpdate.bind(this)
            this.logoDelete = this.logoDelete.bind(this)
            this.logoFetch = this.logoFetch.bind(this)
            this.logoUpload = this.logoUpload.bind(this)
            this.noteCreate = this.noteCreate.bind(this)
            this.noteDelete = this.noteDelete.bind(this)
            this.noteList = this.noteList.bind(this)
        }

        public async aresLookup(ico: string): Promise<interfaces.AresResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/ares/${encodeURIComponent(ico)}`)
            return await resp.json() as interfaces.AresResult
        }

        public async contactCreate(customerId: string, params: controllers.ContactCreateRequest): Promise<controllers.ContactCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers/${encodeURIComponent(customerId)}/contacts`, JSON.stringify(params))
            return await resp.json() as controllers.ContactCreateResponse
        }

        public async contactDelete(id: string): Promise<controllers.ContactDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/customer-contacts/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.ContactDeleteResponse
        }

        public async contactList(customerId: string): Promise<controllers.ContactListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customers/${encodeURIComponent(customerId)}/contacts`)
            return await resp.json() as controllers.ContactListResponse
        }

        public async contactUpdate(id: string, params: controllers.ContactUpdateRequest): Promise<controllers.ContactUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/customer-contacts/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as controllers.ContactUpdateResponse
        }

        public async customerCreate(params: controllers.CustomerCreateRequest): Promise<controllers.CustomerCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers`, JSON.stringify(params))
            return await resp.json() as controllers.CustomerCreateResponse
        }

        public async customerDelete(id: string): Promise<controllers.CustomerDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/customers/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.CustomerDeleteResponse
        }

        public async customerGet(id: string): Promise<controllers.CustomerGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customers/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.CustomerGetResponse
        }

        public async customerList(params: controllers.CustomerListRequest): Promise<controllers.CustomerListResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                country: params.country,
                label:   params.label,
                search:  params.search,
                status:  params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customers`, undefined, {query})
            return await resp.json() as controllers.CustomerListResponse
        }

        public async customerUpdate(id: string, params: controllers.CustomerUpdateRequest): Promise<controllers.CustomerUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/customers/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as controllers.CustomerUpdateResponse
        }

        public async documentContent(id: string): Promise<controllers.DocumentContentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customer-documents/${encodeURIComponent(id)}/content`)
            return await resp.json() as controllers.DocumentContentResponse
        }

        public async documentCreate(customerId: string, params: controllers.DocumentCreateRequest): Promise<controllers.DocumentCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers/${encodeURIComponent(customerId)}/documents`, JSON.stringify(params))
            return await resp.json() as controllers.DocumentCreateResponse
        }

        public async documentDelete(id: string): Promise<controllers.DocumentDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/customer-documents/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.DocumentDeleteResponse
        }

        public async documentList(customerId: string): Promise<controllers.DocumentListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customers/${encodeURIComponent(customerId)}/documents`)
            return await resp.json() as controllers.DocumentListResponse
        }

        public async invoiceCreate(customerId: string, params: controllers.InvoiceCreateRequest): Promise<controllers.InvoiceCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers/${encodeURIComponent(customerId)}/invoices`, JSON.stringify(params))
            return await resp.json() as controllers.InvoiceCreateResponse
        }

        public async invoiceDelete(id: string): Promise<controllers.InvoiceDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/customer-invoices/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.InvoiceDeleteResponse
        }

        public async invoiceList(customerId: string): Promise<controllers.InvoiceListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customers/${encodeURIComponent(customerId)}/invoices`)
            return await resp.json() as controllers.InvoiceListResponse
        }

        public async invoiceUpdate(id: string, params: controllers.InvoiceUpdateRequest): Promise<controllers.InvoiceUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/customer-invoices/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as controllers.InvoiceUpdateResponse
        }

        public async logoDelete(id: string): Promise<controllers.LogoDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/customers/${encodeURIComponent(id)}/logo`)
            return await resp.json() as controllers.LogoDeleteResponse
        }

        public async logoFetch(id: string): Promise<controllers.LogoFetchResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers/${encodeURIComponent(id)}/logo/fetch`)
            return await resp.json() as controllers.LogoFetchResponse
        }

        public async logoUpload(id: string, params: controllers.LogoUploadRequest): Promise<controllers.LogoUploadResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers/${encodeURIComponent(id)}/logo/upload`, JSON.stringify(params))
            return await resp.json() as controllers.LogoUploadResponse
        }

        public async noteCreate(customerId: string, params: controllers.NoteCreateRequest): Promise<controllers.NoteCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/customers/${encodeURIComponent(customerId)}/notes`, JSON.stringify(params))
            return await resp.json() as controllers.NoteCreateResponse
        }

        public async noteDelete(id: string): Promise<controllers.NoteDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/customer-notes/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.NoteDeleteResponse
        }

        public async noteList(customerId: string): Promise<controllers.NoteListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/customers/${encodeURIComponent(customerId)}/notes`)
            return await resp.json() as controllers.NoteListResponse
        }
    }
}

export namespace extraction {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.extractDocument = this.extractDocument.bind(this)
            this.extractInvoice = this.extractInvoice.bind(this)
            this.extractQuote = this.extractQuote.bind(this)
            this.extractText = this.extractText.bind(this)
            this.pipelineExtractHbl = this.pipelineExtractHbl.bind(this)
            this.pipelineExtractMbl = this.pipelineExtractMbl.bind(this)
            this.pipelinePrepare = this.pipelinePrepare.bind(this)
        }

        public async extractDocument(params: controllers.ExtractDocumentRequest): Promise<interfaces.ExtractionResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/document`, JSON.stringify(params))
            return await resp.json() as interfaces.ExtractionResult
        }

        public async extractInvoice(params: controllers.ExtractInvoiceRequest): Promise<interfaces.ExtractionResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/invoice`, JSON.stringify(params))
            return await resp.json() as interfaces.ExtractionResult
        }

        public async extractQuote(params: controllers.ExtractQuoteRequest): Promise<interfaces.ExtractionResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/quote`, JSON.stringify(params))
            return await resp.json() as interfaces.ExtractionResult
        }

        public async extractText(params: controllers.ExtractTextRequest): Promise<interfaces.ExtractionResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/text`, JSON.stringify(params))
            return await resp.json() as interfaces.ExtractionResult
        }

        public async pipelineExtractHbl(params: controllers.PipelineExtractHblRequest): Promise<interfaces.ExtractHblResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/pipeline/extract-hbl-batch`, JSON.stringify(params))
            return await resp.json() as interfaces.ExtractHblResult
        }

        public async pipelineExtractMbl(params: controllers.PipelineExtractMblRequest): Promise<interfaces.ExtractMblResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/pipeline/extract-mbl`, JSON.stringify(params))
            return await resp.json() as interfaces.ExtractMblResult
        }

        public async pipelinePrepare(params: controllers.PipelinePrepareRequest): Promise<interfaces.PrepareResult> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/extraction/pipeline/prepare`, JSON.stringify(params))
            return await resp.json() as interfaces.PrepareResult
        }
    }
}

export namespace invoicing {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.invoicingAddCharge = this.invoicingAddCharge.bind(this)
            this.invoicingDeleteCharge = this.invoicingDeleteCharge.bind(this)
            this.invoicingGenerate = this.invoicingGenerate.bind(this)
            this.invoicingGet = this.invoicingGet.bind(this)
            this.invoicingUpdateCharge = this.invoicingUpdateCharge.bind(this)
            this.invoicingUpsertBillingOverride = this.invoicingUpsertBillingOverride.bind(this)
            this.invoicingUpsertBillingSettings = this.invoicingUpsertBillingSettings.bind(this)
            this.invoicingUpsertCost = this.invoicingUpsertCost.bind(this)
        }

        public async invoicingAddCharge(shipmentId: string, params: controllers.AddChargeRequest): Promise<controllers.AddChargeResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invoicing/${encodeURIComponent(shipmentId)}/additional`, JSON.stringify(params))
            return await resp.json() as controllers.AddChargeResponse
        }

        public async invoicingDeleteCharge(shipmentId: string, chargeId: string): Promise<controllers.DeleteChargeResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/invoicing/${encodeURIComponent(shipmentId)}/additional/${encodeURIComponent(chargeId)}`)
            return await resp.json() as controllers.DeleteChargeResponse
        }

        public async invoicingGenerate(shipmentId: string, params: controllers.GenerateInvoiceRequest): Promise<controllers.GenerateInvoiceResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invoicing/${encodeURIComponent(shipmentId)}/generate`, JSON.stringify(params))
            return await resp.json() as controllers.GenerateInvoiceResponse
        }

        public async invoicingGet(shipmentId: string): Promise<controllers.InvoicingGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/invoicing/${encodeURIComponent(shipmentId)}`)
            return await resp.json() as controllers.InvoicingGetResponse
        }

        public async invoicingUpdateCharge(shipmentId: string, chargeId: string, params: controllers.UpdateChargeRequest): Promise<controllers.UpdateChargeResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/invoicing/${encodeURIComponent(shipmentId)}/additional/${encodeURIComponent(chargeId)}`, JSON.stringify(params))
            return await resp.json() as controllers.UpdateChargeResponse
        }

        public async invoicingUpsertBillingOverride(shipmentId: string, params: controllers.UpsertBillingOverrideRequest): Promise<controllers.UpsertBillingOverrideResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invoicing/${encodeURIComponent(shipmentId)}/billing/overrides`, JSON.stringify(params))
            return await resp.json() as controllers.UpsertBillingOverrideResponse
        }

        public async invoicingUpsertBillingSettings(shipmentId: string, params: controllers.UpsertBillingSettingsRequest): Promise<controllers.UpsertBillingSettingsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invoicing/${encodeURIComponent(shipmentId)}/billing`, JSON.stringify(params))
            return await resp.json() as controllers.UpsertBillingSettingsResponse
        }

        public async invoicingUpsertCost(shipmentId: string, params: controllers.UpsertCostRequest): Promise<controllers.UpsertCostResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/invoicing/${encodeURIComponent(shipmentId)}/costs`, JSON.stringify(params))
            return await resp.json() as controllers.UpsertCostResponse
        }
    }
}

export namespace quotes {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.quoteAllocateRef = this.quoteAllocateRef.bind(this)
            this.quoteAttachmentContent = this.quoteAttachmentContent.bind(this)
            this.quoteAttachmentCreate = this.quoteAttachmentCreate.bind(this)
            this.quoteAttachmentDelete = this.quoteAttachmentDelete.bind(this)
            this.quoteAttachmentList = this.quoteAttachmentList.bind(this)
            this.quoteCreate = this.quoteCreate.bind(this)
            this.quoteDelete = this.quoteDelete.bind(this)
            this.quoteDuplicate = this.quoteDuplicate.bind(this)
            this.quoteGet = this.quoteGet.bind(this)
            this.quoteList = this.quoteList.bind(this)
            this.quoteNextRef = this.quoteNextRef.bind(this)
            this.quoteUpdate = this.quoteUpdate.bind(this)
        }

        public async quoteAllocateRef(quoteNumber: string): Promise<controllers.QuoteAllocateRefResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/quotes/${encodeURIComponent(quoteNumber)}/allocate-ref`)
            return await resp.json() as controllers.QuoteAllocateRefResponse
        }

        public async quoteAttachmentContent(method: "GET", quoteNumber: string, id: string, body?: RequestInit["body"], options?: CallParameters): Promise<globalThis.Response> {
            return this.baseClient.callAPI(method, `/quotes/${encodeURIComponent(quoteNumber)}/attachments/${encodeURIComponent(id)}/content`, body, options)
        }

        public async quoteAttachmentCreate(quoteNumber: string, params: controllers.QuoteAttachmentCreateRequest): Promise<controllers.QuoteAttachmentCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/quotes/${encodeURIComponent(quoteNumber)}/attachments`, JSON.stringify(params))
            return await resp.json() as controllers.QuoteAttachmentCreateResponse
        }

        public async quoteAttachmentDelete(quoteNumber: string, attachmentId: string): Promise<controllers.QuoteAttachmentDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/quotes/${encodeURIComponent(quoteNumber)}/attachments/${encodeURIComponent(attachmentId)}`)
            return await resp.json() as controllers.QuoteAttachmentDeleteResponse
        }

        public async quoteAttachmentList(quoteNumber: string): Promise<controllers.QuoteAttachmentListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/quotes/${encodeURIComponent(quoteNumber)}/attachments`)
            return await resp.json() as controllers.QuoteAttachmentListResponse
        }

        public async quoteCreate(params: controllers.QuoteCreateRequest): Promise<controllers.QuoteCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/quotes`, JSON.stringify(params))
            return await resp.json() as controllers.QuoteCreateResponse
        }

        public async quoteDelete(quoteNumber: string): Promise<controllers.QuoteDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/quotes/${encodeURIComponent(quoteNumber)}`)
            return await resp.json() as controllers.QuoteDeleteResponse
        }

        public async quoteDuplicate(params: controllers.QuoteDuplicateRequest): Promise<controllers.QuoteDuplicateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/quote-duplicate`, JSON.stringify(params))
            return await resp.json() as controllers.QuoteDuplicateResponse
        }

        public async quoteGet(quoteNumber: string): Promise<controllers.QuoteGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/quotes/${encodeURIComponent(quoteNumber)}`)
            return await resp.json() as controllers.QuoteGetResponse
        }

        public async quoteList(params: controllers.QuoteListRequest): Promise<controllers.QuoteListResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:         params.limit === undefined ? undefined : String(params.limit),
                offset:        params.offset === undefined ? undefined : String(params.offset),
                sortDirection: params.sortDirection === undefined ? undefined : String(params.sortDirection),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/quotes`, undefined, {query})
            return await resp.json() as controllers.QuoteListResponse
        }

        public async quoteNextRef(): Promise<controllers.QuoteNextRefResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/quote-next-ref`)
            return await resp.json() as controllers.QuoteNextRefResponse
        }

        public async quoteUpdate(quoteNumber: string, params: controllers.QuoteUpdateRequest): Promise<controllers.QuoteUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/quotes/${encodeURIComponent(quoteNumber)}`, JSON.stringify(params))
            return await resp.json() as controllers.QuoteUpdateResponse
        }
    }
}

export namespace sales {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.prefGet = this.prefGet.bind(this)
            this.prefSet = this.prefSet.bind(this)
            this.termsCreate = this.termsCreate.bind(this)
            this.termsDelete = this.termsDelete.bind(this)
            this.termsGet = this.termsGet.bind(this)
            this.termsList = this.termsList.bind(this)
            this.termsUpdate = this.termsUpdate.bind(this)
        }

        public async prefGet(prefKey: string): Promise<controllers.PrefGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/sales-prefs/${encodeURIComponent(prefKey)}`)
            return await resp.json() as controllers.PrefGetResponse
        }

        public async prefSet(prefKey: string, params: controllers.PrefSetRequest): Promise<controllers.PrefSetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/sales-prefs/${encodeURIComponent(prefKey)}`, JSON.stringify(params))
            return await resp.json() as controllers.PrefSetResponse
        }

        public async termsCreate(params: controllers.TermsCreateRequest): Promise<controllers.TermsCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/terms-conditions`, JSON.stringify(params))
            return await resp.json() as controllers.TermsCreateResponse
        }

        public async termsDelete(id: string): Promise<controllers.TermsDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/terms-conditions/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.TermsDeleteResponse
        }

        public async termsGet(id: string): Promise<controllers.TermsGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/terms-conditions/${encodeURIComponent(id)}`)
            return await resp.json() as controllers.TermsGetResponse
        }

        public async termsList(): Promise<controllers.TermsListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/terms-conditions`)
            return await resp.json() as controllers.TermsListResponse
        }

        public async termsUpdate(id: string, params: controllers.TermsUpdateRequest): Promise<controllers.TermsUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/terms-conditions/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as controllers.TermsUpdateResponse
        }
    }
}

export namespace shipments {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.attachmentContent = this.attachmentContent.bind(this)
            this.attachmentCreate = this.attachmentCreate.bind(this)
            this.attachmentDelete = this.attachmentDelete.bind(this)
            this.attachmentList = this.attachmentList.bind(this)
            this.commentCreate = this.commentCreate.bind(this)
            this.commentDelete = this.commentDelete.bind(this)
            this.commentList = this.commentList.bind(this)
            this.shipmentCreate = this.shipmentCreate.bind(this)
            this.shipmentDashboard = this.shipmentDashboard.bind(this)
            this.shipmentDelete = this.shipmentDelete.bind(this)
            this.shipmentGet = this.shipmentGet.bind(this)
            this.shipmentLinkMasterJob = this.shipmentLinkMasterJob.bind(this)
            this.shipmentList = this.shipmentList.bind(this)
            this.shipmentNextJobNumber = this.shipmentNextJobNumber.bind(this)
            this.shipmentUnlinkMasterJob = this.shipmentUnlinkMasterJob.bind(this)
            this.shipmentUpdate = this.shipmentUpdate.bind(this)
            this.taskList = this.taskList.bind(this)
            this.taskUpsert = this.taskUpsert.bind(this)
        }

        /**
         * Streams the raw file bytes. Inline (viewable) only for allow-listed safe types;
         * otherwise served as an attachment download.
         * Path: /shipments/:shipmentId/attachments/:id/content
         */
        public async attachmentContent(method: "GET", shipmentId: string, id: string, body?: RequestInit["body"], options?: CallParameters): Promise<globalThis.Response> {
            return this.baseClient.callAPI(method, `/shipments/${encodeURIComponent(shipmentId)}/attachments/${encodeURIComponent(id)}/content`, body, options)
        }

        public async attachmentCreate(shipmentId: string, params: controllers.AttachmentCreateRequest): Promise<controllers.AttachmentCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/shipments/${encodeURIComponent(shipmentId)}/attachments`, JSON.stringify(params))
            return await resp.json() as controllers.AttachmentCreateResponse
        }

        public async attachmentDelete(shipmentId: string, attachmentId: string): Promise<controllers.AttachmentDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/shipments/${encodeURIComponent(shipmentId)}/attachments/${encodeURIComponent(attachmentId)}`)
            return await resp.json() as controllers.AttachmentDeleteResponse
        }

        public async attachmentList(shipmentId: string): Promise<controllers.AttachmentListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments/${encodeURIComponent(shipmentId)}/attachments`)
            return await resp.json() as controllers.AttachmentListResponse
        }

        public async commentCreate(shipmentId: string, params: controllers.CommentCreateRequest): Promise<controllers.CommentCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/shipments/${encodeURIComponent(shipmentId)}/comments`, JSON.stringify(params))
            return await resp.json() as controllers.CommentCreateResponse
        }

        public async commentDelete(shipmentId: string, commentId: string): Promise<controllers.CommentDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/shipments/${encodeURIComponent(shipmentId)}/comments/${encodeURIComponent(commentId)}`)
            return await resp.json() as controllers.CommentDeleteResponse
        }

        public async commentList(shipmentId: string): Promise<controllers.CommentListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments/${encodeURIComponent(shipmentId)}/comments`)
            return await resp.json() as controllers.CommentListResponse
        }

        public async shipmentCreate(params: controllers.ShipmentCreateRequest): Promise<controllers.ShipmentCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/shipments`, JSON.stringify(params))
            return await resp.json() as controllers.ShipmentCreateResponse
        }

        public async shipmentDashboard(): Promise<controllers.DashboardResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments/dashboard`)
            return await resp.json() as controllers.DashboardResponse
        }

        /**
         * Soft-delete only — the row and all details are kept (deletedAt is set) so the
         * job reference can never be reused. The actor is derived server-side from the
         * authenticated user and recorded in the audit log.
         */
        public async shipmentDelete(shipmentId: string): Promise<controllers.ShipmentDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/shipments/${encodeURIComponent(shipmentId)}`)
            return await resp.json() as controllers.ShipmentDeleteResponse
        }

        public async shipmentGet(shipmentId: string): Promise<controllers.ShipmentGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments/${encodeURIComponent(shipmentId)}`)
            return await resp.json() as controllers.ShipmentGetResponse
        }

        public async shipmentLinkMasterJob(shipmentId: string, params: controllers.LinkMasterJobRequest): Promise<controllers.LinkMasterJobResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/shipments/${encodeURIComponent(shipmentId)}/master-job`, JSON.stringify(params))
            return await resp.json() as controllers.LinkMasterJobResponse
        }

        public async shipmentList(params: controllers.ShipmentListRequest): Promise<controllers.ShipmentListResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                customerId:    params.customerId,
                limit:         params.limit === undefined ? undefined : String(params.limit),
                offset:        params.offset === undefined ? undefined : String(params.offset),
                search:        params.search,
                sortDirection: params.sortDirection === undefined ? undefined : String(params.sortDirection),
                status:        params.status,
                statusBucket:  params.statusBucket,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments`, undefined, {query})
            return await resp.json() as controllers.ShipmentListResponse
        }

        /**
         * Returns the next CZ job number, accounting for archived shipments so a
         * reference is never reused.
         */
        public async shipmentNextJobNumber(): Promise<controllers.NextJobNumberResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments/next-job-number`)
            return await resp.json() as controllers.NextJobNumberResponse
        }

        public async shipmentUnlinkMasterJob(shipmentId: string): Promise<controllers.UnlinkMasterJobResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/shipments/${encodeURIComponent(shipmentId)}/master-job`)
            return await resp.json() as controllers.UnlinkMasterJobResponse
        }

        public async shipmentUpdate(shipmentId: string, params: controllers.ShipmentUpdateRequest): Promise<controllers.ShipmentUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/shipments/${encodeURIComponent(shipmentId)}`, JSON.stringify(params))
            return await resp.json() as controllers.ShipmentUpdateResponse
        }

        public async taskList(shipmentId: string): Promise<controllers.TaskListResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shipments/${encodeURIComponent(shipmentId)}/tasks`)
            return await resp.json() as controllers.TaskListResponse
        }

        public async taskUpsert(shipmentId: string, params: controllers.TaskUpsertRequest): Promise<controllers.TaskUpsertResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/shipments/${encodeURIComponent(shipmentId)}/tasks`, JSON.stringify(params))
            return await resp.json() as controllers.TaskUpsertResponse
        }
    }
}

export namespace warehouse {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.warehouseCreate = this.warehouseCreate.bind(this)
            this.warehouseDelete = this.warehouseDelete.bind(this)
            this.warehouseList = this.warehouseList.bind(this)
            this.warehouseSectionGet = this.warehouseSectionGet.bind(this)
            this.warehouseSectionUpsert = this.warehouseSectionUpsert.bind(this)
            this.warehouseUpdate = this.warehouseUpdate.bind(this)
        }

        public async warehouseCreate(params: controllers.WarehouseCreateRequest): Promise<controllers.WarehouseCreateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/warehouse`, JSON.stringify(params))
            return await resp.json() as controllers.WarehouseCreateResponse
        }

        public async warehouseDelete(taskId: string): Promise<controllers.WarehouseDeleteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/warehouse/${encodeURIComponent(taskId)}`)
            return await resp.json() as controllers.WarehouseDeleteResponse
        }

        public async warehouseList(params: controllers.WarehouseListRequest): Promise<controllers.WarehouseListResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                shipmentId: params.shipmentId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/warehouse`, undefined, {query})
            return await resp.json() as controllers.WarehouseListResponse
        }

        public async warehouseSectionGet(shipmentId: string, section: string): Promise<controllers.WarehouseSectionGetResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/warehouse/sections/${encodeURIComponent(shipmentId)}/${encodeURIComponent(section)}`)
            return await resp.json() as controllers.WarehouseSectionGetResponse
        }

        public async warehouseSectionUpsert(shipmentId: string, section: string, params: controllers.WarehouseSectionUpsertRequest): Promise<controllers.WarehouseSectionUpsertResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/warehouse/sections/${encodeURIComponent(shipmentId)}/${encodeURIComponent(section)}`, JSON.stringify(params))
            return await resp.json() as controllers.WarehouseSectionUpsertResponse
        }

        public async warehouseUpdate(taskId: string, params: controllers.WarehouseUpdateRequest): Promise<controllers.WarehouseUpdateResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/warehouse/${encodeURIComponent(taskId)}`, JSON.stringify(params))
            return await resp.json() as controllers.WarehouseUpdateResponse
        }
    }
}

export namespace controllers {
    export interface AddChargeRequest {
        invoiceNumber?: string
        vendor?: string
        description?: string
        estAmount?: string
        estCurrency?: string
        realAmount?: string
        realCurrency?: string
        sortOrder?: number
    }

    export interface AddChargeResponse {
        charge: interfaces.AdditionalChargeItem
    }

    export interface AttachmentCreateRequest {
        fileName: string
        fileSize: number
        fileType: string
        contentBase64?: string
    }

    export interface AttachmentCreateResponse {
        attachment: interfaces.AttachmentItem
    }

    export interface AttachmentDeleteResponse {
        ok: boolean
    }

    export interface AttachmentListResponse {
        attachments: interfaces.AttachmentItem[]
    }

    export interface AuthLoginRequest {
        email: string
        password: string
    }

    export interface AuthLoginResponse {
        token: string
        user: services.AuthUserInfo
    }

    /**
     * NOTE: kept auth:false with manual JWT verification (not gateway auth:true) so the
     * generated web client's request shape stays unchanged. Security is equivalent — the
     * token is cryptographically verified here. Migrate to auth:true + getAuthData() when
     * the Encore client is regenerated.
     */
    export interface AuthMeRequest {
        authorization: string
    }

    export interface AuthMeResponse {
        user: services.AuthUserInfo
    }

    export interface AutomationLogsResponse {
        logs: interfaces.AutomationLogItem[]
    }

    export interface AutomationTriggerRequest {
        shipmentId: string
        column: string
        oldValue: string
        newValue: string
        shipmentData?: { [key: string]: string }
    }

    export interface AutomationTriggerResponse {
        actions: string[]
        logs: interfaces.AutomationLogItem[]
    }

    /**
     * Plain interface — do not import drizzle $inferSelect types into controllers.
     */
    export interface ColumnTemplateItem {
        id: string
        userId: string
        name: string
        columns: string[]
    }

    export interface CommentCreateRequest {
        message: string
    }

    export interface CommentCreateResponse {
        comment: interfaces.CommentItem
    }

    export interface CommentDeleteResponse {
        ok: boolean
    }

    export interface CommentListResponse {
        comments: interfaces.CommentItem[]
    }

    export interface CompanyListResponse {
        companies: services.CompanyInfo[]
    }

    export interface CompanyProvisionRequest {
        companyName: string
        companySlug: string
        adminEmail: string
        adminPassword: string
        adminName?: string
    }

    export interface CompanyProvisionResponse {
        companyId: string
        admin: services.AuthUserInfo
    }

    export interface CompanyUserCreateRequest {
        email: string
        password: string
        displayName?: string
        role?: string
    }

    export interface CompanyUserCreateResponse {
        user: services.AuthUserInfo
    }

    export interface CompanyUserDeleteResponse {
        ok: boolean
    }

    export interface CompanyUserUpdateRequest {
        displayName?: string
        role?: string
        password?: string
    }

    export interface CompanyUserUpdateResponse {
        user: services.AuthUserInfo
    }

    export interface CompanyUsersListResponse {
        users: services.AuthUserInfo[]
    }

    export interface ContactCreateRequest {
        name: string
        email?: string
        phone?: string
        role?: string
        isMain?: boolean
    }

    export interface ContactCreateResponse {
        contact: interfaces.ContactItem
    }

    export interface ContactDeleteResponse {
        ok: boolean
    }

    export interface ContactListResponse {
        data: interfaces.ContactItem[]
    }

    export interface ContactUpdateRequest {
        customerId: string
        name?: string
        email?: string
        phone?: string
        role?: string
        isMain?: boolean
    }

    export interface ContactUpdateResponse {
        contact: interfaces.ContactItem
    }

    export interface CustomerCreateRequest {
        ico: string
    }

    export interface CustomerCreateResponse {
        customer: interfaces.CustomerItem
    }

    export interface CustomerDeleteResponse {
        ok: boolean
    }

    export interface CustomerGetResponse {
        customer: interfaces.CustomerItem
    }

    export interface CustomerListRequest {
        /**
         * Server-side filtering.
         */
        search?: string

        status?: string
        label?: string
        country?: string
    }

    export interface CustomerListResponse {
        data: interfaces.CustomerItem[]
    }

    export interface CustomerUpdateRequest {
        companyName?: string
        dic?: string
        status?: string
        salesOwner?: string
        label?: string
        creditLimit?: number
        currency?: string
        paymentTerms?: string
        freightPaymentTerms?: string
        dutyPaymentTerms?: string
        companyWebsite?: string
        city?: string
        country?: string
        registeredAddress?: string
        nace?: string
        totalRevenue?: number
        totalProfit?: number
        totalShipments?: number
        lastActivityDate?: string
    }

    export interface CustomerUpdateResponse {
        customer: interfaces.CustomerItem
    }

    export interface DashboardResponse {
        kpis: {
            totalActive: number
            waitingUnload: number
            sailedNotOrdered: number
            sailedOrdered: number
            waitingDeparture: number
            missingSailing: number
        }
        deadlines: DeadlineGroup[]
    }

    export interface DeadlineGroup {
        period: string
        items: DeadlineItem[]
    }

    export interface DeadlineItem {
        id: string
        jobNumber: string
        shipper: string
        consignee: string
        status: string
        field: string
        date: string
    }

    export interface DeleteChargeResponse {
        ok: boolean
    }

    export interface DeleteColumnTemplateRequest {
        authorization: string
    }

    export interface DeleteColumnTemplateResponse {
        ok: boolean
    }

    export interface DocumentContentResponse {
        fileName: string
        fileType: string
        fileData: string
    }

    export interface DocumentCreateRequest {
        name: string
        type?: string
        fileName?: string
        fileType?: string
        fileSize?: number
        fileData?: string
    }

    export interface DocumentCreateResponse {
        document: interfaces.DocumentItem
    }

    export interface DocumentDeleteResponse {
        ok: boolean
    }

    export interface DocumentListResponse {
        data: interfaces.DocumentItem[]
    }

    export interface ExtractDocumentRequest {
        fileBase64: string
        fileName: string
    }

    export interface ExtractInvoiceRequest {
        fileBase64: string
        fileName: string
    }

    export interface ExtractQuoteRequest {
        fileBase64: string
        fileName: string
    }

    export interface ExtractTextRequest {
        text: string
        destination: string
    }

    export interface GenerateInvoiceRequest {
        jobNumber: string
        invoiceType: string
        billingCurrency: string
        totalAmount: string
    }

    export interface GenerateInvoiceResponse {
        invoice: interfaces.GeneratedInvoiceItem
    }

    export interface InvoiceCreateRequest {
        invoiceNumber: string
        amount?: number
        dueDate?: string
        status?: string
        issuedAt?: string
    }

    export interface InvoiceCreateResponse {
        invoice: interfaces.InvoiceItem
    }

    export interface InvoiceDeleteResponse {
        ok: boolean
    }

    export interface InvoiceListResponse {
        data: interfaces.InvoiceItem[]
    }

    export interface InvoiceUpdateRequest {
        invoiceNumber?: string
        amount?: number
        dueDate?: string
        status?: string
        issuedAt?: string
    }

    export interface InvoiceUpdateResponse {
        invoice: interfaces.InvoiceItem
    }

    export interface InvoicingGetResponse {
        costs: interfaces.InvoiceCostItem[]
        additionalCharges: interfaces.AdditionalChargeItem[]
        billingSettings: interfaces.BillingSettingsItem | null
        billingOverrides: interfaces.BillingOverrideItem[]
        generatedInvoices: interfaces.GeneratedInvoiceItem[]
    }

    export interface LinkMasterJobRequest {
        mczNumber: string
    }

    export interface LinkMasterJobResponse {
        shipment: interfaces.ShipmentItem
    }

    export interface ListColumnTemplatesRequest {
        authorization: string
    }

    export interface ListColumnTemplatesResponse {
        templates: ColumnTemplateItem[]
    }

    export interface LogoDeleteResponse {
        customer: interfaces.CustomerItem
    }

    export interface LogoFetchResponse {
        customer: interfaces.CustomerItem
    }

    export interface LogoUploadRequest {
        dataUrl: string
    }

    export interface LogoUploadResponse {
        customer: interfaces.CustomerItem
    }

    export interface NextJobNumberResponse {
        jobNumber: string
    }

    export interface NoteCreateRequest {
        type?: string
        content: string
        author?: string
    }

    export interface NoteCreateResponse {
        note: interfaces.NoteItem
    }

    export interface NoteDeleteResponse {
        ok: boolean
    }

    export interface NoteListResponse {
        data: interfaces.NoteItem[]
    }

    export interface PipelineExtractHblRequest {
        sessionId: string
        batchIndex: number
    }

    export interface PipelineExtractMblRequest {
        sessionId: string
    }

    export interface PipelinePrepareRequest {
        fileBase64: string
        fileName: string
    }

    export interface PrefGetResponse {
        value: any | null
    }

    export interface PrefSetRequest {
        value: any
    }

    export interface PrefSetResponse {
        ok: boolean
    }

    export interface QuoteAllocateRefResponse {
        ref: string
    }

    export interface QuoteAttachmentCreateRequest {
        fileName: string
        fileSize: number
        fileType: string
        contentBase64?: string
    }

    export interface QuoteAttachmentCreateResponse {
        attachment: interfaces.QuoteAttachmentItem
    }

    export interface QuoteAttachmentDeleteResponse {
        ok: boolean
    }

    export interface QuoteAttachmentListResponse {
        attachments: interfaces.QuoteAttachmentItem[]
    }

    export interface QuoteCreateRequest {
        quoteNumber: string
        data?: any
        terms?: string
    }

    export interface QuoteCreateResponse {
        quote: interfaces.QuoteItem
    }

    export interface QuoteDeleteResponse {
        ok: boolean
    }

    export interface QuoteDuplicateRequest {
        baseRef: string
        data?: any
    }

    export interface QuoteDuplicateResponse {
        quoteNumber: string
        quote: interfaces.QuoteItem
    }

    export interface QuoteGetResponse {
        quote: interfaces.QuoteItem
    }

    export interface QuoteListRequest {
        limit?: number
        offset?: number
        sortDirection?: "asc" | "desc"
    }

    export interface QuoteListResponse {
        pagination: {
            total: number
            offset: number
            limit: number
        }
        data: interfaces.QuoteItem[]
    }

    export interface QuoteNextRefResponse {
        ref: string
    }

    export interface QuoteUpdateRequest {
        data?: any
        terms?: string
    }

    export interface QuoteUpdateResponse {
        quote: interfaces.QuoteItem
    }

    export interface ShipmentCreateRequest {
        jobNumber: string
        shipper?: string
        consignee?: string
        pol?: string
        pod?: string
        destination?: string
        tradeDirection?: string
        loadType?: string
        status?: string
        customsStatus?: string
        vessel?: string
        voyage?: string
        shippingLine?: string
        bookingNumber?: string
        typeOfPackages?: string
        serviceName?: string
        invoicingStatus?: string
        personalReference?: string
        agent?: string
        hsCode?: string
        cargoDescription?: string
        incotermOrigin?: string
        incotermDestination?: string
        houseBolNumber?: string
        masterBolNumber?: string
        houseBolType?: string
        masterBolType?: string
        pcs?: string
        totalWeightTons?: string
        totalVolumeCbm?: string
        cargoOrigin?: string
        commercialInvoiceValue?: string
        /**
         * Meta
         */
        shipmentsDate?: string

        department?: string
        personInCharge?: string
        holidayCover?: string
        /**
         * Customer
         */
        customerId?: string

        shipperId?: string
        consigneeId?: string
        customer?: string
        customerPic?: string
        customerReference?: string
        /**
         * Commercial parties
         */
        pickupAddress?: string

        deliveryAddress?: string
        shipperContact?: string
        consigneeContact?: string
        shipperOpeningFrom?: string
        shipperOpeningTo?: string
        consigneeOpeningFrom?: string
        consigneeOpeningTo?: string
        /**
         * Status / mode
         */
        freeComments?: string

        freightMode?: string
        /**
         * Agent
         */
        agentPic?: string

        serviceType?: string
        /**
         * Insurance
         */
        insurance?: string

        /**
         * Dates
         */
        estimatedDeparture?: string

        estimatedArrival?: string
        cargoReadinessDate?: string
        pickupDate?: string
        pickupTime?: string
        closingDate?: string
        etaWarehouse?: string
        plannedDeliveryDate?: string
        plannedDeliveryTime?: string
        /**
         * Commercial
         */
        commercialInvoice?: string

        creditCheck?: string
        approvedBy?: string
        bookingConfirmation?: string
        customsProcedure?: string
        equipmentDelivery?: string
        supplierPic?: string
        /**
         * Compliance
         */
        vgm?: string

        shippingInstructions?: string
        ams?: string
        isf?: string
        bolDraft?: string
        /**
         * Switch BoL
         */
        switchBol?: string

        switchBolApprovedBy?: string
        switchBolNumber?: string
        /**
         * Containers (4 sets)
         */
        containerCount1?: string

        containerLength1?: string
        containerType1?: string
        containerCount2?: string
        containerLength2?: string
        containerType2?: string
        containerCount3?: string
        containerLength3?: string
        containerType3?: string
        containerCount4?: string
        containerLength4?: string
        containerType4?: string
        /**
         * Detail rows (containers + cargo lines)
         */
        containers?: interfaces.ContainerLine[]

        cargoItems?: interfaces.CargoItemLine[]
        cargoDimensions?: interfaces.CargoDimensionLine[]
        /**
         * Quote
         */
        salesNumber?: string

        selling?: string
        buying?: string
        quoteValidity?: string
        validityStatus?: string
        salesPerson?: string
        /**
         * Other
         */
        claim?: string

        createdBy?: string
    }

    export interface ShipmentCreateResponse {
        shipment: interfaces.ShipmentItem
    }

    export interface ShipmentDeleteResponse {
        ok: boolean
    }

    export interface ShipmentGetResponse {
        shipment: interfaces.ShipmentItem
    }

    export interface ShipmentListRequest {
        limit?: number
        offset?: number
        sortDirection?: "asc" | "desc"
        customerId?: string
        /**
         * Server-side filtering.
         */
        status?: string

        /**
         * Coarse UI status bucket: active | in-transit | customs | delivered
         */
        statusBucket?: string

        search?: string
    }

    export interface ShipmentListResponse {
        pagination: {
            total: number
            offset: number
            limit: number
        }
        data: interfaces.ShipmentItem[]
    }

    export interface ShipmentUpdateRequest {
        shipper?: string
        consignee?: string
        pol?: string
        pod?: string
        destination?: string
        countryCode?: string
        origin?: string
        tradeDirection?: string
        loadType?: string
        status?: string
        customsStatus?: string
        vessel?: string
        voyage?: string
        shippingLine?: string
        bookingNumber?: string
        typeOfPackages?: string
        serviceName?: string
        invoicingStatus?: string
        personalReference?: string
        agent?: string
        hsCode?: string
        cargoDescription?: string
        incotermOrigin?: string
        incotermDestination?: string
        houseBolNumber?: string
        masterBolNumber?: string
        houseBolType?: string
        masterBolType?: string
        pcs?: string
        totalWeightTons?: string
        totalVolumeCbm?: string
        cargoOrigin?: string
        commercialInvoiceValue?: string
        /**
         * Meta
         */
        shipmentsDate?: string

        department?: string
        personInCharge?: string
        holidayCover?: string
        /**
         * Customer
         */
        customerId?: string

        shipperId?: string
        consigneeId?: string
        customer?: string
        customerPic?: string
        customerReference?: string
        /**
         * Commercial parties
         */
        pickupAddress?: string

        deliveryAddress?: string
        shipperContact?: string
        consigneeContact?: string
        shipperOpeningFrom?: string
        shipperOpeningTo?: string
        consigneeOpeningFrom?: string
        consigneeOpeningTo?: string
        /**
         * Status / mode
         */
        freeComments?: string

        freightMode?: string
        /**
         * Agent
         */
        agentPic?: string

        serviceType?: string
        /**
         * Insurance
         */
        insurance?: string

        /**
         * Dates
         */
        estimatedDeparture?: string

        estimatedArrival?: string
        actualDeparture?: string
        actualArrival?: string
        cargoReadinessDate?: string
        pickupDate?: string
        pickupTime?: string
        closingDate?: string
        etaWarehouse?: string
        plannedDeliveryDate?: string
        plannedDeliveryTime?: string
        /**
         * Commercial
         */
        commercialInvoice?: string

        creditCheck?: string
        approvedBy?: string
        bookingConfirmation?: string
        customsProcedure?: string
        equipmentDelivery?: string
        supplierPic?: string
        /**
         * Compliance
         */
        vgm?: string

        shippingInstructions?: string
        ams?: string
        isf?: string
        bolDraft?: string
        /**
         * Switch BoL
         */
        switchBol?: string

        switchBolApprovedBy?: string
        switchBolNumber?: string
        /**
         * Containers (4 sets)
         */
        containerCount1?: string

        containerLength1?: string
        containerType1?: string
        containerCount2?: string
        containerLength2?: string
        containerType2?: string
        containerCount3?: string
        containerLength3?: string
        containerType3?: string
        containerCount4?: string
        containerLength4?: string
        containerType4?: string
        /**
         * Detail rows (containers + cargo lines; each replaces the shipment's set when present)
         */
        containers?: interfaces.ContainerLine[]

        cargoItems?: interfaces.CargoItemLine[]
        cargoDimensions?: interfaces.CargoDimensionLine[]
        /**
         * Quote
         */
        salesNumber?: string

        selling?: string
        buying?: string
        quoteValidity?: string
        validityStatus?: string
        salesPerson?: string
        /**
         * Other
         */
        claim?: string

        createdBy?: string
    }

    export interface ShipmentUpdateResponse {
        shipment: interfaces.ShipmentItem
    }

    export interface TaskListResponse {
        tasks: interfaces.TaskItem[]
    }

    export interface TaskUpsertRequest {
        taskKey: string
        completed: boolean
    }

    export interface TaskUpsertResponse {
        task: interfaces.TaskItem
    }

    export interface TermsCreateRequest {
        name: string
    }

    export interface TermsCreateResponse {
        terms: interfaces.TermsConditionItem
    }

    export interface TermsDeleteResponse {
        ok: boolean
    }

    export interface TermsGetResponse {
        terms: interfaces.TermsConditionItem
    }

    export interface TermsListResponse {
        data: interfaces.TermsConditionItem[]
    }

    export interface TermsUpdateRequest {
        name?: string
        includes?: string
        excludes?: string
    }

    export interface TermsUpdateResponse {
        terms: interfaces.TermsConditionItem
    }

    export interface UnlinkMasterJobResponse {
        shipment: interfaces.ShipmentItem
    }

    export interface UpdateChargeRequest {
        invoiceNumber?: string
        vendor?: string
        description?: string
        estAmount?: string
        estCurrency?: string
        realAmount?: string
        realCurrency?: string
    }

    export interface UpdateChargeResponse {
        charge: interfaces.AdditionalChargeItem
    }

    export interface UpsertBillingOverrideRequest {
        rowKey: string
        billingAmount: string
    }

    export interface UpsertBillingOverrideResponse {
        override: interfaces.BillingOverrideItem
    }

    export interface UpsertBillingSettingsRequest {
        billingCurrency?: string
        roe?: string
        quoteRef?: string
    }

    export interface UpsertBillingSettingsResponse {
        billingSettings: interfaces.BillingSettingsItem
    }

    export interface UpsertColumnTemplateRequest {
        authorization: string
        name: string
        columns: string[]
    }

    export interface UpsertColumnTemplateResponse {
        template: ColumnTemplateItem
    }

    export interface UpsertCostRequest {
        category: string
        estAmount?: string
        estCurrency?: string
        realAmount?: string
        realCurrency?: string
        invoiceNumber?: string
        vendor?: string
    }

    export interface UpsertCostResponse {
        cost: interfaces.InvoiceCostItem
    }

    export interface UserCreateRequest {
        email: string
        password: string
        displayName?: string
        role?: string
    }

    export interface UserCreateResponse {
        user: services.AuthUserInfo
    }

    export interface UserDeleteResponse {
        ok: boolean
    }

    export interface UserUpdateRequest {
        displayName?: string
        role?: string
        password?: string
    }

    export interface UserUpdateResponse {
        user: services.AuthUserInfo
    }

    export interface UsersListResponse {
        users: services.AuthUserInfo[]
    }

    export interface WarehouseCreateRequest {
        taskId: string
        shipmentId?: string
    }

    export interface WarehouseCreateResponse {
        task: interfaces.WarehouseTaskItem
    }

    export interface WarehouseDeleteResponse {
        ok: boolean
    }

    export interface WarehouseListRequest {
        shipmentId?: string
    }

    export interface WarehouseListResponse {
        tasks: interfaces.WarehouseTaskItem[]
    }

    export interface WarehouseSectionGetResponse {
        section: interfaces.WarehouseSectionItem | null
    }

    export interface WarehouseSectionUpsertRequest {
        data: any
    }

    export interface WarehouseSectionUpsertResponse {
        section: interfaces.WarehouseSectionItem
    }

    export interface WarehouseUpdateRequest {
        type?: string
        priority?: string
        status?: string
        assignee?: string
        dueDate?: string
        cargo?: string
        weight?: string
        notes?: string
        data?: any
    }

    export interface WarehouseUpdateResponse {
        task: interfaces.WarehouseTaskItem
    }
}

export namespace interfaces {
    export interface AdditionalChargeItem {
        id: string
        shipmentId: string
        invoiceNumber: string
        vendor: string
        description: string
        estAmount: string | null
        estCurrency: string
        realAmount: string | null
        realCurrency: string
        sortOrder: number
    }

    export interface AresResult {
        ico: string
        dic: string
        companyName: string
        legalForm: string
        registeredAddress: string
        city: string
        country: string
        companyStatus: string
        registrationDate: string
        nace: string
    }

    export interface AttachmentItem {
        id: string
        shipmentId: string
        fileName: string
        fileSize: number
        fileType: string
        storageKey: string
        createdAt: string
    }

    export interface AutomationLogItem {
        id: string
        shipmentId: string
        ruleName: string
        action: string
        details: any
        triggeredById: string | null
        createdAt: string
    }

    export interface BillingOverrideItem {
        id: string
        shipmentId: string
        rowKey: string
        billingAmount: string | null
    }

    export interface BillingSettingsItem {
        id: string
        shipmentId: string
        billingCurrency: string
        roe: string
        quoteRef: string
    }

    /**
     * A dimension line of the Cargo Dimensions card: `pieces` identical pieces of
     * L×W×H (cm) and weightPerPcKg each. Volume per piece is derived, never stored.
     */
    export interface CargoDimensionLine {
        containerId?: string | null
        pieces: string
        lengthCm: string
        widthCm: string
        heightCm: string
        weightPerPcKg: string
        packageType: string
        stackable: string
    }

    /**
     * A goods line of the Cargo Description card. No volume by design — volume only
     * exists on dimension lines. containerId is null for containerless cargo (LCL/air).
     */
    export interface CargoItemLine {
        containerId?: string | null
        cargoDescription: string
        hsCode: string
        pieces: string
        packageType: string
        grossWeight: string
        commercialInvoiceValue: string
        currency: string
    }

    export interface CommentItem {
        id: string
        shipmentId: string
        authorId: string
        message: string
        createdAt: string
    }

    export interface ContactItem {
        id: string
        customerId: string
        name: string
        email: string
        phone: string
        role: string
        isMain: boolean
        createdAt: string
        updatedAt: string
    }

    export interface ContainerLine {
        /**
         * Stable row id. Present on reads; clients echo it back on updates so cargo
         * lines keep pointing at the same container. Absent on brand-new rows.
         */
        id?: string

        containerNumber: string
        sealNumber: string
        type: string
        /**
         * Derived from type on the server (20' → 1, 40' → 2); client values are ignored.
         */
        teu: string

        packages: string
        packageType: string
        grossWeight: string
        volume: string
    }

    export interface CustomerItem {
        id: string
        ico: string
        dic: string
        companyName: string
        legalForm: string
        registeredAddress: string
        city: string
        country: string
        companyStatus: string
        registrationDate: string
        nace: string
        dataSource: string
        lastRegistryUpdate: string
        status: string
        salesOwner: string
        label: string
        creditLimit: number
        currency: string
        paymentTerms: string
        freightPaymentTerms: string
        dutyPaymentTerms: string
        companyWebsite: string
        logoData: string
        logoSource: string
        logoUpdatedAt: string
        totalRevenue: number
        totalProfit: number
        totalShipments: number
        lastActivityDate: string
        createdAt: string
        updatedAt: string
    }

    export interface DocumentItem {
        id: string
        customerId: string
        name: string
        type: string
        fileName: string
        fileType: string
        fileSize: number
        createdAt: string
        updatedAt: string
    }

    export interface ExtractHblResult {
        shipments: { [key: string]: string }[]
        batchIndex: number
        totalBatches: number
        totalExtracted: number
        done: boolean
    }

    export interface ExtractMblResult {
        mblInfo: { [key: string]: string } | null
        message?: string
    }

    export interface ExtractionResult {
        extracted: { [key: string]: string }
        fieldCount: number
        method: "text" | "vision"
        fileName: string
    }

    export interface GeneratedInvoiceItem {
        id: string
        shipmentId: string
        invoiceNumber: string
        invoiceType: string
        billingCurrency: string
        totalAmount: string
        createdAt: string
    }

    export interface InvoiceCostItem {
        id: string
        shipmentId: string
        category: string
        estAmount: string | null
        estCurrency: string
        realAmount: string | null
        realCurrency: string
        invoiceNumber: string
        vendor: string
    }

    export interface InvoiceItem {
        id: string
        customerId: string
        invoiceNumber: string
        amount: number
        dueDate: string
        status: string
        issuedAt: string
        createdAt: string
        updatedAt: string
    }

    export interface NoteItem {
        id: string
        customerId: string
        type: string
        content: string
        author: string
        createdAt: string
        updatedAt: string
    }

    export interface PrepareResult {
        sessionId: string
        pageCount: number
        classification: { [key: string]: number }
        pages: {
            pageNum: number
            type: string
        }[]
        fileName: string
    }

    export interface QuoteAttachmentItem {
        id: string
        quoteNumber: string
        fileName: string
        fileSize: number
        fileType: string
        storageKey: string
        createdAt: string
    }

    export interface QuoteItem {
        id: string
        quoteNumber: string
        data: any
        terms: string
        createdAt: string
        updatedAt: string
    }

    export interface ShipmentItem {
        id: string
        jobNumber: string
        shipper: string
        consignee: string
        personalReference: string
        containerNumber: string
        sealNumber: string
        typeOfPackages: string
        serviceName: string
        invoicingStatus: string
        bookingNumber: string
        loadType: string
        shippingLine: string
        pol: string
        pod: string
        destination: string
        hsCode: string
        cargoDescription: string
        houseBolNumber: string
        masterBolNumber: string
        houseBolType: string
        masterBolType: string
        vessel: string
        voyage: string
        pcs: string
        totalWeightTons: string | null
        totalVolumeCbm: string | null
        cargoOrigin: string
        countryCode: string
        origin: string
        estimatedDeparture: string | null
        estimatedArrival: string | null
        actualDeparture: string | null
        actualArrival: string | null
        tradeDirection: string
        agent: string
        incotermOrigin: string
        incotermDestination: string
        commercialInvoiceValue: string | null
        status: string
        customsStatus: string
        masterJobId: string | null
        masterJobMczNumber: string | null
        /**
         * Meta
         */
        shipmentsDate: string | null

        department: string
        personInCharge: string
        holidayCover: string
        /**
         * Customer
         */
        customerId: string | null

        shipperId: string | null
        consigneeId: string | null
        customer: string
        customerPic: string
        customerReference: string
        /**
         * Commercial parties
         */
        pickupAddress: string

        deliveryAddress: string
        shipperContact: string
        consigneeContact: string
        shipperOpeningFrom: string
        shipperOpeningTo: string
        consigneeOpeningFrom: string
        consigneeOpeningTo: string
        /**
         * Status / mode
         */
        freeComments: string

        freightMode: string
        /**
         * Agent
         */
        agentPic: string

        serviceType: string
        /**
         * Insurance
         */
        insurance: string

        /**
         * Dates
         */
        cargoReadinessDate: string | null

        pickupDate: string | null
        pickupTime: string
        closingDate: string | null
        etaWarehouse: string | null
        plannedDeliveryDate: string | null
        plannedDeliveryTime: string
        /**
         * Commercial
         */
        commercialInvoice: string

        creditCheck: string
        approvedBy: string
        bookingConfirmation: string
        customsProcedure: string
        equipmentDelivery: string
        supplierPic: string
        /**
         * Compliance
         */
        vgm: string

        shippingInstructions: string
        ams: string
        isf: string
        bolDraft: string
        /**
         * Switch BoL
         */
        switchBol: string

        switchBolApprovedBy: string
        switchBolNumber: string
        /**
         * Containers (4 sets)
         */
        containerCount1: string

        containerLength1: string
        containerType1: string
        containerCount2: string
        containerLength2: string
        containerType2: string
        containerCount3: string
        containerLength3: string
        containerType3: string
        containerCount4: string
        containerLength4: string
        containerType4: string
        /**
         * Container / cargo detail rows (own tables, ordered by position)
         */
        containers: ContainerLine[] | null

        cargoItems: CargoItemLine[] | null
        cargoDimensions: CargoDimensionLine[] | null
        /**
         * Read-only projections computed from the rows above (never stored).
         * containerNumber/sealNumber/pcs/typeOfPackages/hsCode/cargoDescription are
         * also overridden with computed values when detail rows exist.
         */
        containerTypeSummary: string

        totalTeu: string
        totalGrossWeightKg: string
        totalVolumeM3: string
        civByCurrency: string
        /**
         * Quote
         */
        salesNumber: string

        selling: string
        buying: string
        quoteValidity: string
        validityStatus: string
        salesPerson: string
        /**
         * Other
         */
        claim: string

        createdBy: string
        createdAt: string
        updatedAt: string
    }

    export interface TaskItem {
        id: string
        shipmentId: string
        taskKey: string
        completed: boolean
        completedAt: string | null
        completedById: string | null
    }

    export interface TermsConditionItem {
        id: string
        name: string
        includes: string
        excludes: string
        createdAt: string
        updatedAt: string
    }

    export interface WarehouseSectionItem {
        id: string
        shipmentId: string
        section: string
        data: any
        createdAt: string
        updatedAt: string
    }

    export interface WarehouseTaskItem {
        id: string
        taskId: string
        shipmentId: string | null
        type: string
        priority: string
        status: string
        assignee: string
        dueDate: string
        cargo: string
        weight: string
        notes: string
        data: any
        createdAt: string
        updatedAt: string
    }
}

export namespace services {
    export interface AuthUserInfo {
        id: string
        companyId: string
        email: string
        displayName: string
        role: string
    }

    export interface CompanyInfo {
        id: string
        name: string
        slug: string
        createdAt: string
    }
}



function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "method" | "body" | "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | lib.AuthParams
  | Promise<lib.AuthParams | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "shipment-tracker-ysci-Generated-TS-Client (Encore/v1.58.0)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: lib.AuthParams | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        return this.callAPI(method, path, body, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
            method,
            body: body ?? null,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}
